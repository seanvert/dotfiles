#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc
export PATH="/home/arch/flutter/flutter/bin:$PATH"
. "$HOME/.cargo/env"

export PATH="/home/arch/.local/share/solana/install/active_release/bin:$PATH"
